import React from "react";

import styles from "./styles.module.css";

const Header = () => {
  return (
    <div className={styles.wrapper}>
      <div>VOTE RANKING APP</div>
    </div>
  );
};

export default Header;
